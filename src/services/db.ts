/**
 * Database access is handled via Supabase PostgreSQL from Next.js API routes.
 * See `src/lib/supabase.ts` and `supabase/migrations/`.
 */

export default function connectDB() {
  return null;
}
